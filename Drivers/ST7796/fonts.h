#ifndef __FONTS_H
#define __FONTS_H /* include guard for bitmap font type declarations */

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>

typedef struct {
    const uint8_t  width;
    const uint8_t  height;
    const uint16_t *data;
} FontDef;

/* 32-bit wide font type for glyphs wider than 16 px.
 * Each row is a uint32_t; MSB (bit 31) = leftmost pixel.
 * Bits (31 - width) .. 0 are unused padding on the right.
 */
typedef struct {
    const uint8_t   width;
    const uint8_t   height;
    const uint32_t *data;
} FontDef32;

/*
 * Available fonts.
 * Each row of bitmap data is stored as a uint16_t with the leftmost pixel
 * in bit 15 and the rightmost pixel in bit (16 - width).
 */
extern FontDef Font_7x10;
extern FontDef Font_11x18;
extern FontDef Font_16x26;

/* Consolas 23x49 – large preset name font (42pt), generated by tools/gen_consolas_40.py */
extern FontDef32 Font_Consolas23x49;

/* Consolas 15x35 – data field font (28pt), generated by tools/gen_consolas_40.py */
extern FontDef32 Font_Consolas15x35;

#ifdef __cplusplus
}
#endif

#endif /* __FONTS_H */
